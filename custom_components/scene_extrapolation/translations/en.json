{
  "config": {
    "abort": {
      "already_configured": "Scene Extrapolation is already configured",
      "single_instance_allowed": "Scene Extrapolation is already configured. Open it from the sidebar to add scenes."
    },
    "error": {
      "unknown": "Unexpected error"
    },
    "step": {
      "user": {
        "title": "Scene Extrapolation",
        "description": "Adds Scene Extrapolation to the sidebar. Create and edit extrapolation scenes from there — one integration, as many rooms as you need."
      }
    }
  }
}
